<Window x:Class="WpfApp10.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp10"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="377*"/>
            <RowDefinition Height="31*"/>
            <RowDefinition Height="26*"/>
            <RowDefinition Height="26*"/>
            <RowDefinition Height="26*"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="dataGrid"/>
        <Button Grid.Row="1" Content="Сохранить" Click="SaveFileButton_Click"/>
        <Button Grid.Row="2" Content="Загрузить" Click="OpenFileButton_Click"/>
        <Button Grid.Row="3" Content="Новый" Click="NewFileButton_Click"/>
        <Button Grid.Row="4" Content="Отправить" Click="SendEmailButton_Click"/>
    </Grid>
</Window>
